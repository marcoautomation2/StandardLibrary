use base.UnitTests as UnitTests;
use base.Test as Test;
use base.Void as Void;
use base.Try as Try;
use base.Error as Error;
use base.List as List;
use base.Lists as Lists;
use base.Flows as Flows;
use base.Flow as Flow;
use base.Par as Par;
use base.ParRepr as ParRepr;
use base.Debug as Debug;
use base.Opt as Opt;
use base.Str as Str;
use base.Num as Num;
use base.Nat as Nat;
use base.Int as Int;
use base.Float as Float;
use base.Byte as Byte;
use base.ELists as ELists;
use base.Vars as Vars;
use base.Info as Info;
use base.Map as Map;
use base.Maps as Maps;
use base.Sets as Sets;
use base.ESets as ESets;
use base.Opts as Opts;
use base.True as True;
use base.False as False;
use base.Bool as Bool;
use base.Block as Block;
use base.Continue as Continue;
use base.Break as Break;
use base.OuterReturn as OuterReturn;
use base.Unreachable as Unreachable;
use base.Math as Math;
use base.F as F;
use base.OrderHash as OrderHash;
use base.Order as Order;
use base.OrderHashBy as OrderHashBy;
use base.OrderBy as OrderBy;
use base.Rev as Rev;
use base.DataType as DataType;
use base.DataTypeBy as DataTypeBy;
use base.Pipe as Pipe;
use base.Operation as Operation;
use base.Todo as Todo;
use base.ESet as ESet;
use base.Norm as Norm;
use base.Cache as Cache;
use base.XInt as X;
use base.YInt as Y;
use base.WidthNat as Width;
use base.HeightNat as Height;
use base.Red as Red;
use base.Green as Green;
use base.Blue as Blue;
use base.Alpha as Alpha;
use base.Color as Color;
use base.Hue as Hue;
use base.Saturation as Saturation;
use base.Brightness as Brightness;
use base.Duration as Duration;
use base.Instant as Instant;
use base.Rect as Rect;
use base.Rects as Rects;
use base.ToInfo as ToInfo;
use base.ToInfoBy as ToInfoBy;
use base.Infos as Infos;
use base.InfoValidators as InfoValidators;
use base.Enum as Enum;
use base.ControlFlow as ControlFlow;
use base.Enums as Enums;

MyTests:UnitTests{::
  .test TestByte
  .test TestFloat
  .test TestInt
  .test TestNat
  .test TestNum
  .test TestStr
  .test TestEnum
  .test TestWrappers
  .test GridHitTestTests
  .test GuiLayoutTests
  .test GuiBuilderTests
  .test TestList
  .test TestFlow
  .test TestMap
  .test TestAs
  .test TestRepr
  .test TestLiteralErrors
  .test TestPipe
  .test TestComparators
  .test TestUStr
  .test MathUnitTests
  .test InfoTests
  .test InfoValidatorTests
  .test InfoExtractorTests
  .test TestBlock
  .test TestInfoFromStr
  .test TestDungeon
  .test TestTryGet
  .test TestFlowDesign
  .test TestMaxShapes
  }
